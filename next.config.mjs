/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
         domains : ['localhost' , 'aceternity.com' , 'uplabs.com' , 'www.google.com' , 'assets.materialup.com' , 'www.logo.wine' ,"images.unsplash.com" , "github.blog", "img.freepik.com" ]
    }
};

export default nextConfig;
